{
  "name": "slide",
  "version": "0.1.0",
  "description": "Slide - Nightclub Line-Skip Pass App",
  "private": true,
  "scripts": {
    "dev": "pnpm dev",
    "build": "pnpm build",
    "test": "pnpm test",
    "lint": "pnpm lint",
    "type-check": "pnpm type-check",
    "seed": "pnpm --filter ./scripts seed"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "supabase"
  ],
  "dependencies": {}
}
