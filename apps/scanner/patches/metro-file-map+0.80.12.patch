diff --git a/node_modules/.pnpm/metro-file-map@0.80.12/node_modules/metro-file-map/src/worker.js b/node_modules/.pnpm/metro-file-map@0.80.12/node_modules/metro-file-map/src/worker.js
index 0000000..0000001 100644
--- a/node_modules/.pnpm/metro-file-map@0.80.12/node_modules/metro-file-map/src/worker.js
+++ b/node_modules/.pnpm/metro-file-map@0.80.12/node_modules/metro-file-map/src/worker.js
@@
-const { promises: fsPromises } = require("fs");
-const fs = require("graceful-fs");
+const { promises: fsPromises } = require("fs");
+const nodeFs = require("fs");
+const fs = require("graceful-fs");
@@
-  const getContent = () => {
-    if (content == null) {
-      content = fs.readFileSync(filePath);
-    }
-    return content;
-  };
+  const getContent = () => {
+    if (content == null) {
+      const maxAttempts = 3;
+      let attempt = 0;
+      let lastErr;
+
+      while (attempt < maxAttempts && content == null) {
+        try {
+          content = fs.readFileSync(filePath);
+          break;
+        } catch (err) {
+          lastErr = err;
+          if (err.code === "ETIMEDOUT") {
+            try {
+              content = nodeFs.readFileSync(filePath);
+              break;
+            } catch (fallbackErr) {
+              lastErr = fallbackErr;
+            }
+          }
+        }
+
+        attempt += 1;
+        if (content == null && lastErr && lastErr.code === "ETIMEDOUT" && attempt < maxAttempts) {
+          try {
+            Atomics.wait(new Int32Array(new SharedArrayBuffer(4)), 0, 0, 10);
+          } catch (pauseErr) {}
+        }
+      }
+
+      if (content == null) {
+        if (lastErr) {
+          throw lastErr;
+        }
+        throw new Error(`Failed to read content for ${filePath}`);
+      }
+    }
+    return content;
+  };
