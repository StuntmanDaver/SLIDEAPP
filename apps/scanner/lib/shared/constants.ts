/**
 * Shared constants for Slide Scanner app
 * Bundled from @slide/shared for EAS build compatibility
 */

// Scan Results
export const SCAN_RESULTS = {
  VALID: "VALID",
  USED: "USED",
  EXPIRED: "EXPIRED",
  INVALID: "INVALID",
  REVOKED: "REVOKED",
} as const;
